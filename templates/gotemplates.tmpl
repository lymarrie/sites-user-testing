{{define "city.url" -}}
{{.externalEntityId}}.html
{{- end}}


{{define "city.source" -}}
<html>
    <body>
        <h1> Welcome to - {{.name}}, {{.c_parents}} </h1>
        <p>{{range . }}
            {{ .c_children }}
            {{end}}</p>
            
        <p>{{range . }}
            {{ .c_parents }}
            {{end}}</p>
    </body>
</html>
{{end}}
